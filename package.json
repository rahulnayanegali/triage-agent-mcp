{
  "name": "triage-agent-mcp",
  "version": "0.1.0",
  "description": "AI bug triage MCP server — classifies Sentry issues by severity and routes to owning team",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --import tsx/esm --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2"
  },
  "engines": {
    "node": ">=18.19"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}
