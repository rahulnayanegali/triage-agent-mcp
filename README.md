# triage-agent-mcp

### Usage
```
Claude Code / Claude Desktop
        ↓
Triage MCP Server
  → triage_issue()
  → route_to_team()
  → notify_team()
        ↓
Official Sentry MCP (getsentry/sentry-mcp)
        ↓
Sentry API
```
